specVersion: 0.0.2
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: MasterDeployer
    network: mainnet
    source:
      address: "0x9fe46736679d2d9a65f0992f2272de9f3c7fa6e0"
      abi: MasterDeployer
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - NewPoolCreated
        - OwnershipTransferred
      abis:
        - name: MasterDeployer
          file: ./abis/MasterDeployer.json
      eventHandlers:
        - event: NewPoolCreated(indexed address,indexed address)
          handler: handleNewPoolCreated
        - event: TransferOwnership(indexed address,indexed address)
          handler: handleOwnershipTransferred
        - event: TransferOwnershipClaim(indexed address,indexed address)
          handler: handleTransferOwnershipClaim
      file: ./src/mapping.ts
